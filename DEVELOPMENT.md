# Logbook — Development Guide

Ο τεχνικός οδηγός του Logbook: αρχιτεκτονική, τοπική εκτέλεση, quality gate, διαδικασία release και η τρέχουσα τεχνική κατάσταση. Περιγράφει το προϊόν όπως είναι σήμερα — όχι το ιστορικό του.

| Στοιχείο | Τρέχουσα κατάσταση |
|---|---|
| Έκδοση | **0.3.2** |
| Runtime | Node.js 22 |
| Client | HTML, CSS και JavaScript χωρίς build step στην ανάπτυξη |
| Αποθήκευση | Local-first με `localStorage` και Supabase sync |
| Tests | 345 unit/integration passed, 78 e2e εκτελέσεις σε 10 αρχεία (76 passed, 2 προβλεπόμενα skips) |
| Production | [ddevezoglou.github.io/logbook](https://ddevezoglou.github.io/logbook/) |
| Deployment | GitHub Pages μέσω GitHub Actions |

**Κατάσταση παραγωγής, 6 Σεπτεμβρίου 2026:** εφαρμόστηκαν οι τρεις migrations που εντοπίστηκαν ως εκκρεμείς. Ο τελικός έλεγχος επιβεβαίωσε και τις **8 migrations** του repository, τις προστασίες δεδομένων και τα δικαιώματα. Τα δύο account snapshots παρέμειναν ακριβώς αμετάβλητα. Τα ανοιχτά TODO αφορούν πλέον μόνο τα 11 προαπαιτούμενα εμπορικής διάθεσης του 1.0.

**Έκδοση εργασίας 0.3.2:** διορθώθηκαν τα τρία ευρήματα του [review συγχρονισμού](docs/review-2026-09-06.md): καθυστερημένες απαντήσεις μετά από αποσύνδεση, αποθήκευση φόρμας πάνω σε παλιό state και καταγραφές που γίνονται όσο εκκρεμεί αίτημα sync. Δεν έγινε commit, tag, δημοσίευση ή αλλαγή παραγωγικής βάσης για αυτές τις διορθώσεις.

## Γρήγορη εκκίνηση

Από τον φάκελο του repository:

```powershell
npm.cmd ci
npm.cmd run dev
```

Άνοιξε το:

```text
http://localhost:3001/
```

Μην ανοίγεις το `index.html` ως `file://`. Το authentication, τα callbacks και ο service worker απαιτούν κανονικό web origin.

Για διαφορετική θύρα:

```powershell
node scripts/serve-static.mjs --port 3000
```

Ο τοπικός server στέλνει `Cache-Control: no-store`. Η εφαρμογή αφαιρεί επίσης αυτόματα παλιούς Logbook service workers και caches στο `localhost`, ώστε η ανάπτυξη να μη συνδυάζει αρχεία διαφορετικών εκδόσεων.

## Αρχιτεκτονική

Το Logbook είναι local-first web εφαρμογή. Οι αλλαγές γράφονται πρώτα στη συσκευή και το UI δεν εξαρτάται από συνεχή σύνδεση στο δίκτυο.

```text
Browser UI
   ↓
localStorage ── άμεση τοπική αποθήκευση και offline λειτουργία
   ↓
cloud-sync.js ── merge, revision checks και conflict retry
   ↓
Supabase ── authentication και versioned snapshot ανά χρήστη
```

Κατά την εκκίνηση:

1. Ελέγχεται η αποθηκευμένη συνεδρία.
2. Χωρίς ενεργό login εμφανίζεται μόνο το authentication gate.
3. Online, ολοκληρώνεται ο αρχικός συγχρονισμός πριν φορτωθεί το κύριο UI.
4. Offline, μια cached συνεδρία ανοίγει τα τοπικά δεδομένα και το sync επαναλαμβάνεται όταν επιστρέψει το δίκτυο.

Η ταυτότητα περνά από μηχανή τεσσάρων καταστάσεων — `unknown`, `member`, `offline-member`, `guest` — στο `session-state.js`, και κάθε αλλαγή ταυτότητας περνά από ένα και μόνο σημείο μετάβασης στο `auth.js`. Storage, UI και ο παρατηρήσιμος κύκλος ζωής δεν μπορούν επομένως να διαφωνήσουν για την ενεργή κατάσταση.

Οι ενημερώσεις από το cloud ανανεώνουν το state και τα views μέσα στην ανοιχτή σελίδα, χωρίς `location.reload()` και χωρίς επανεμφάνιση του auth gate. Διατηρούνται η ενεργή σελίδα και οι διαθέσιμες επιλογές του γραφήματος. Όταν υπάρχει μη αποθηκευμένη φόρμα, η ανανέωση αναβάλλεται μέχρι την επόμενη ασφαλή πλοήγηση. Τα browser tests καλύπτουν διαδοχικές ενημερώσεις χωρίς νέο navigation και την απομόνωση λογαριασμών σε Chromium και WebKit.

**Προστασία ταυτόχρονης αποθήκευσης (0.3.2).** Το `data-reconciliation.js` εφαρμόζει μόνο τις αλλαγές από το τελευταίο γνωστό state πάνω στα πιο πρόσφατα δεδομένα. Οι συλλογές και οι ημέρες προγράμματος συγκρίνονται με σταθερά IDs, ενώ τα σετ χωρίς ID παραμένουν μία ενιαία αλλαγή. Σε σύγκρουση του ίδιου πεδίου προτιμάται η τοπική αλλαγή· οι διαγραφές δεν ανασταίνονται. Η αποθήκευση ανοιχτής φόρμας για εγγραφή που διαγράφηκε απομακρυσμένα απορρίπτεται χωρίς να σβήσει το draft. Μετά τη συγχώνευση παραμένει ένα ενεργό πρόγραμμα.

Κάθε sync φέρει τη δική του ταυτότητα συνεδρίας και AbortController. Το sign-out ή η αλλαγή χρήστη ακυρώνει το παλιό αίτημα και απορρίπτει την απάντησή του ακόμη κι αν ο client αγνοήσει το abort. Το token refresh του ίδιου χρήστη δεν ακυρώνει τον συγχρονισμό. Αλλαγές που γίνονται όσο εκκρεμεί read/write διατηρούνται και ανεβαίνουν στον επόμενο κύκλο πριν ολοκληρωθεί το ίδιο sync promise.

## Τι κάνει η εφαρμογή

- Πολλαπλά ανεξάρτητα προγράμματα και μικρόκυκλοι 3–10 ημερών.
- Προγραμματισμένες και ελεύθερες προπονήσεις με ασκήσεις, σετ, επαναλήψεις, βάρη, cues και σχόλια. Η αποθήκευση γίνεται σε κανονικοποιημένα kg, ενώ η καταχώριση και η εμφάνιση υποστηρίζουν συνεπή εναλλαγή kg/lb.
- Ιστορικό με σελιδοποίηση ανά 30, ασφαλή εξαγωγή σε CSV, προσωπικά ρεκόρ, στατιστικά, γραφήματα προόδου και επιβράβευση μόνο για καταγεγραμμένη σημερινή προπόνηση.
- Προφίλ αθλητή και ελληνικό, αγγλικό, γαλλικό και γερμανικό interface, με ελεγμένη φυσική διατύπωση και πλήρη κάλυψη των translation keys.
- Supabase Auth με email/κωδικό ή Google, και συγχρονισμός πολλών συσκευών με optimistic revision checks, αυτόματο merge και conflict retry.
- **Λειτουργία επισκέπτη** ως απόφαση της συσκευής και όχι λογαριασμός: δύο μόνιμα τοπικά κλειδιά (`logbookGuest`, `logbookGuestReminderAt`), καμία κλήση στο cloud όσο ο χρήστης παραμένει επισκέπτης, καμία κλειδωμένη λειτουργία, και μια non-blocking `aria-live="polite"` λωρίδα υπενθύμισης που επανέρχεται κάθε επτά ημέρες.
- **Αυτόματη μεταφορά επισκέπτη σε λογαριασμό:** στην πρώτη είσοδο ή εγγραφή με email ή Google, τα δεδομένα της συσκευής συγχωνεύονται αυτόματα με τα δεδομένα του λογαριασμού. Δεν υπάρχει dialog επιλογής και δεν χάνονται δεδομένα αν αποτύχει η πρώτη προσπάθεια· το προσωρινό `logbookGuestImportPending` καθαρίζεται μόνο μετά από επιτυχημένο sync.
- Installable PWA με offline shell και responsive mobile UI.
- Εκτυπώσιμη σελίδα προπόνησης και πλάνου, μία προπόνηση ανά φύλλο, με `body:has(dialog[open])` ώστε να τυπώνεται ό,τι είναι ανοιχτό.
- Νυχτερινή εμφάνιση με ρητό διακόπτη στο μενού, προεπιλογή OFF και εφαρμογή πριν το πρώτο paint.
- Δημόσια [πολιτική απορρήτου](privacy.html) που καλύπτει ρητά τα ημερήσια snapshots ως 30ήμερη λειτουργική διατήρηση.
- Αυτόματη μεταφορά παλιότερων τοπικών δεδομένων στο τρέχον μοντέλο.

## Πώς είναι φτιαγμένο

Οι μηχανικές αρχές που πρέπει να επιβιώσουν κάθε επόμενη αλλαγή.

**Απομόνωση δεδομένων ανά λογαριασμό.** Στο επιτυχές sign-out καθαρίζονται τα κοινά domain keys, ενώ διατηρούνται ο owner marker και το user-scoped recovery cache. Ο `owner ≠ id` έλεγχος στο `performSync` αναγνωρίζει την εναλλαγή χρήστη και δεν αφήνει τοπικά δεδομένα να μεταναστεύσουν σε άλλον λογαριασμό. E2E σενάρια ελέγχουν την εναλλαγή χρήστη και την είσοδο επισκέπτη μετά από αποσύνδεση.

**Η ενεργή προπόνηση αντέχει τον κύκλο ζωής του κινητού.** Κάθε αλλαγή πεδίου γράφεται συγχρονισμένα στο τοπικό `logbookWorkoutDraft`, χωρίς debounce ή αναμονή δικτύου. Το ίδιο ισχύει μετά από προσθήκη, αντιγραφή και επιβεβαιωμένη αφαίρεση σετ/ασκήσεων. Τα `visibilitychange:hidden`, `pagehide` και `beforeunload` παραμένουν πρόσθετη προστασία, χωρίς να απαιτούνται για τη διατήρηση της τελευταίας πληκτρολόγησης. Εμφανίζεται ένδειξη επιτυχούς τοπικής αποθήκευσης ή επίμονο μήνυμα αποτυχίας αν ο browser αρνηθεί το write. Επανέρχονται ασκήσεις, σετ, τρόποι βάρους, σχόλια, ημερομηνία και ενεργή κάρτα· ακόμη και σημειώσεις χωρίς ασκήσεις. Το draft μένει στη συγκεκριμένη συσκευή, ενώ η ολοκληρωμένη προπόνηση συγχρονίζεται όταν επιστρέψει το δίκτυο.

Σε offline εκκίνηση με cached session ανοίγουν αμέσως τα τοπικά δεδομένα ακόμη και αν έχει λήξει το access token, μόνο όταν ο αποθηκευμένος owner ταυτίζεται με τον λογαριασμό. Διαφορετικός ή άγνωστος owner απαιτεί επανασύνδεση, χωρίς έκθεση ή διαγραφή δεδομένων. Ο Supabase client αρχικοποιείται με την επανασύνδεση. Αυτό προϋποθέτει διαθέσιμο cached app shell για άνοιγμα χωρίς δίκτυο. Κάθε renderer διατηρεί τον αρχικό owner του: μετά από απώλεια συνεδρίας ή αλλαγή χρήστη δεν ξαναγράφει το πρόχειρο, ούτε ανασταίνει δεδομένα που καθάρισε το ρητό sign-out. Η απώλεια συνεδρίας διατηρεί το ήδη γραμμένο draft για επιστροφή στον ίδιο λογαριασμό. Ολοκλήρωση, ρητή απόρριψη και ρητό sign-out εξακολουθούν να το καθαρίζουν.

Τα `tests/workout-draft.test.mjs` και `tests/supabase-client.test.mjs` ελέγχουν άμεση διατήρηση χωρίς συμβάν κλεισίματος, τρεις ασκήσεις σε ελεύθερη/προγραμματισμένη προπόνηση, αποτυχία storage και απομόνωση λογαριασμών. Το `e2e/mobile.spec.mjs` ελέγχει πραγματικό κλείσιμο σελίδας και νέο renderer με τρεις ασκήσεις/εννέα σετ σε Chromium και WebKit. Στο Chromium ελέγχει επιπλέον offline επανεκκίνηση με ληγμένο token, ολοκλήρωση χωρίς δίκτυο και αποστολή μίας προπόνησης μετά την επανασύνδεση. Παραμένει απαραίτητη η δοκιμή εγκατεστημένης PWA σε φυσικά Android/iPhone· η διαγραφή δεδομένων του browser ή η αφαίρεση της εφαρμογής δεν καλύπτονται από το τοπικό draft.

**Επαλήθευση 6/9/2026, μετά την ενίσχυση του draft:** `npm.cmd run check` exit 0, **76 e2e passed / 2 skipped**. Επανεκτέλεση όλου του unit suite μετά τον τελικό έλεγχο offline owner: **345 passed / 0 failed**. Τελική επανεκτέλεση των browser σεναρίων offline boot και τριών ασκήσεων: **3 passed / 1 αναμενόμενο WebKit skip**, exit 0. Performance: **1.334.115 / 1.400.000 bytes**, **LCP 1.468 / 3.500 ms**, **TBT 15 / 500 ms**. Logs: `output/release-check-workout-durability.log`, `output/workout-durability-unit-final.log`, `output/workout-draft-e2e-final.log`. Οι προηγούμενες μετρήσεις παρακάτω παραμένουν ως ιστορικό του αρχικού review.

**Η αποτυχία του αρχικού συγχρονισμού έχει πραγματική έξοδο.** Κάθε cloud request ακυρώνεται μετά από 15 δευτερόλεπτα και η επανάληψη ξεκινά νέο request, αντί να περιμένει ένα Promise που έχει ήδη κολλήσει. Αν καθυστερήσει το `getSession()`, το `logbook:session-state` ξεκινά ελεγχόμενα τον συγχρονισμό χωρίς να δημιουργεί δεύτερο παράλληλο sync. Σε αποτυχία εμφανίζεται καθαρό μήνυμα χωρίς εσωτερικά `<DIAG watchdog…>` στοιχεία, και τα «ΠΡΟΣΠΑΘΕΙΑ ΞΑΝΑ» και «ΑΠΟΣΥΝΔΕΣΗ» παραμένουν πάνω από κάθε interaction-blocking layer και είναι πάντοτε πατήσιμα. Η αποσύνδεση καθαρίζει τοπικά το auth token — μαζί με τυχόν chunked keys — χωρίς να περιμένει το δίκτυο. Αν, και μόνο αν, ο owner marker της συσκευής ταυτίζεται με το `id` της ενεργής συνεδρίας, προσφέρεται επιπλέον το «ΑΝΟΙΓΜΑ ΣΤΗ ΣΥΣΚΕΥΗ». Ο έλεγχος ιδιοκτησίας γίνεται και κατά την εμφάνιση και κατά το πάτημα, ώστε να μην ανοίξουν δεδομένα άλλου λογαριασμού.

**Οι διαγραφές είναι δεδομένα.** Διαγραμμένες προπονήσεις και προγράμματα μένουν στο local-first snapshot ως ελάχιστα tombstones `{ id, deletedAt }`. Το runtime state και το UI τα φιλτράρουν, ενώ το merge τα προτιμά από stale ζωντανές εγγραφές του ίδιου `id`, ώστε μια άλλη συσκευή να μην μπορεί να αναστήσει διαγραμμένο περιεχόμενο.

**Ένα token layer, τέσσερα overrides — όχι τέσσερα θέματα.** Η ημέρα ορίζεται στο `:root` του `tokens.css`· η νύχτα, το `prefers-contrast: more` με στόχο AAA (7:1) και το `@media print` είναι overrides του ίδιου layer. Ρόλοι που κρύβονταν κάτω από κοινό χρώμα είναι χωριστά tokens: μελάνι έναντι μελανί επιφάνειας (`--ink` / `--ink-surface` / `--on-ink`), κείμενο πάνω σε accent και σε χρυσό (`--on-accent` / `--on-gold`), και βάθος έναντι χρώματος στις ημιδιαφανείς χρήσεις (`--ink-rgb` / `--shadow-rgb` / `--veil-rgb`). Το invariant φυλάσσεται μηχανικά από το `tests/design-tokens.test.mjs`: κάθε hex έξω από `:root` block αποτυγχάνει το build.

**Τρία breakpoints.** `≤600px` «ένα χέρι», `≤850px` «χωρίς δεύτερη στήλη», `≤1000px` «χωρίς περιθώριο», με μία ρητή εξαίρεση περιεχομένου στα `≤380px` για τη γραμμή σετ. Κάθε νέο breakpoint χρειάζεται γραμμένη αιτιολόγηση ή δεν μπαίνει.

**Collision-aware αρχική στο desktop.** Σε πλάτος τουλάχιστον 1000px, η προεπιλεγμένη θέση της κινητής κάρτας προγράμματος ελέγχεται ταυτόχρονα απέναντι στο quote και στα κύρια controls με πραγματικά bounding boxes και ασφαλές διάκενο, ακόμη και όταν εμφανίζεται η ημερήσια σφραγίδα. Μια μετακίνηση για αποφυγή ενός στοιχείου δεν επιτρέπεται να επικαλύψει κάποιο άλλο. Οι ρητά αποθηκευμένες θέσεις του χρήστη παραμένουν δική του επιλογή.

**Modular ES αρχιτεκτονική** με σαφή όρια για storage/migrations, routines, sessions, progress, DOM-free παραγωγή του Ιστορικού και του γραφήματος, και κοινά UI primitives. Κοινό SVG sprite για τα navigation controls και σταθερά `message.NNNN` i18n IDs για DOM, ARIA και σύνθετα labels. Κάθε ελληνικό source ζει μία μόνο φορά στον κατάλογο: η αναζήτηση ανά source και η αναζήτηση ανά id δεν μπορούν να δώσουν διαφορετική μετάφραση για την ίδια λέξη, και ένα test κλειδώνει τη μοναδικότητα.

**Cascade με μηχανικό όριο.** Η κάρτα λογαριασμού, τα dialogs και το interaction-hardening layer ζουν στο `dialogs.css`· το print override μένει τελευταίο στο `views.css`.

**Ασφάλεια στα δεδομένα.** Row Level Security σε κάθε πίνακα, `revoke all` στα αδρανή phase-1 tables, `security definer` με `set search_path = ''` σε κάθε function, επικυρωμένο (`validate constraint`) όριο payload κάτω από 2 MiB, και snapshots αόρατα από το Data API. Στον client: κανονικοποίηση αριθμητικών τιμών, escaping του περιεχομένου χρήστη πριν από κάθε απόδοση σε HTML, βαθύ validation των local/cloud payloads, και allowlist των data URLs του avatar.

**CSP** με `default-src 'self'`, `object-src 'none'`, `base-uri 'self'` και `form-action 'self'`. Κανένα `script-src 'unsafe-inline'`.

**Η λειτουργία επισκέπτη έχει σαφή κύκλο ζωής.** Όσο ο χρήστης είναι επισκέπτης, τα δεδομένα μένουν αποκλειστικά στη συσκευή. Μόλις δημιουργήσει λογαριασμό ή συνδεθεί — είτε με email είτε με Google — το ίδιο pipeline συγχρονισμού εισάγει αυτόματα τα δεδομένα της συσκευής στον λογαριασμό και τα συγχωνεύει με ό,τι υπάρχει ήδη στο cloud. Το παλιό dialog «συγχώνευση / μόνο cloud / ακύρωση» έχει αφαιρεθεί πλήρως από markup, styles, events και μεταφράσεις. Η σημαία εκκρεμούς εισαγωγής παραμένει σε αποτυχία και καθαρίζεται μόνο μετά την επιτυχία, ώστε το retry να είναι ασφαλές. Δεν προστίθεται περιεχόμενο υποδοχής, σελίδα-δείγμα ή προτεινόμενο πρόγραμμα, ούτε αλλάζει η αφετηρία της υπενθύμισης.

**Αγγλικά που μένουν αγγλικά.** Ένα μικρό, κλειστό σύνολο κειμένων δεν περνά από το i18n επειδή δεν είναι κείμενο διεπαφής αλλά τυπωμένο στοιχείο της σελίδας: οι σφραγίδες («MEMBER», «EARN IT», «PERSONAL RECORDS», «GROWTH HAPPENS IN THE REST, NOT THE DOING», «LOGBOOK / TODAY»), τα eyebrow labels του τετραδίου («VOL 1», «PAGE 001») και το `QUOTE OF THE DAY` στο `#daily-quote-label`. Είναι σκόπιμη απόφαση προϊόντος, όχι κενό μετάφρασης: διαβάζονται ως μελάνι πάνω στο χαρτί και είναι ίδια και στις τέσσερις γλώσσες. Το ίδιο το απόφθεγμα ακολουθεί ήδη τον κανόνα: το `quotes.js` ορίζει ρητά το `text` ως «το αγγλικό κείμενο που εμφανίζεται σε όλες τις γλώσσες», οπότε μεταφρασμένο eyebrow πάνω από αμετάφραστο σώμα θα ήταν η μόνη ασυνέπεια. Νέο κείμενο μπαίνει σε αυτή τη λίστα μόνο αν είναι σφραγίδα ή τυπογραφικό σημάδι· κάθε άλλο κείμενο του UI παίρνει `data-i18n-id`.

**Προσβασιμότητα ως συμβόλαιο.** `inert`/`aria-hidden` στο κρυμμένο auth gate, focus και live-region contracts, axe έλεγχοι στα κρίσιμα mobile flows, και τεκμηριωμένο [keyboard-only και screen-reader walkthrough](docs/accessibility-walkthrough.md).

**Privacy-safe error tracking** για sync, PWA και πραγματικά unhandled client failures, μόνο με allowlisted τεχνικά metadata, rate limit 10 συμβάντων ανά ώρα στη βάση και διατήρηση 30 ημερών. Στη λειτουργία επισκέπτη έως 10 αποστειρωμένα events μένουν σε τοπική ουρά και αποστέλλονται μετά την πρώτη επιτυχημένη σύνδεση.

**Self-hosted και pinned** Supabase browser bundle `2.110.7`, με καταγεγραμμένο SHA-256, άδεια MIT και offline/integrity tests.

**Release hygiene.** Ενιαίο quality gate που αποκλείει debug logging και test artifacts από τον production κώδικα και τα releases. Ελεγχόμενο version bump που ενημερώνει μαζί package metadata, UI version, service-worker cache, tests και τεκμηρίωση.

## Χάρτης βασικών αρχείων

| Αρχείο/φάκελος | Ευθύνη |
|---|---|
| `index.html` | Βασικό markup, dialogs και script loading |
| `tokens.css` | Το token layer: η ημέρα στο `:root` και τα overrides της για νύχτα και `prefers-contrast: more` |
| `base.css` | Reset, τυπογραφία, auth gate και τα πλέγματα που κρατούν κάθε view |
| `components.css` | Κάρτες, controls, μενού, σφραγίδες και προφίλ |
| `dialogs.css` | Κάρτα λογαριασμού, account/confirmation surfaces και το τελικό interaction-hardening layer |
| `views.css` | Προσαρμογές ανά view και ανά συσκευή, και το print override που πρέπει να μένει τελευταίο |
| `app.js` | Κύρια εφαρμογή και UI orchestration πάνω από τα domain modules |
| `modules/storage-migrations.js` | Typed local storage, ασφαλείς εγγραφές και migrations παλιών δεδομένων |
| `modules/exercises.js` | Προσωπική βιβλιοθήκη, σταθερές ταυτότητες, ασφαλής αντιστοίχιση παλιών ασκήσεων και migration backup |
| `modules/routines.js` | Μοντέλο προγραμμάτων, μικρόκυκλοι και αντιστοίχιση ημερών |
| `modules/sessions.js` | Ημερομηνίες, μονάδες βάρους, validation και ασφαλής CSV μορφοποίηση |
| `modules/progress-chart.js` | Καθαρή, DOM-free παραγωγή του SVG του γραφήματος προόδου |
| `modules/progress-rewards.js` | Συγκρίσεις επιδόσεων και καμπύλες γραφημάτων |
| `modules/history.js` | DOM-free παραγωγή του Ιστορικού και των καρτών προπόνησης |
| `modules/session-templates.js` | DOM-free παραγωγή καρτών άσκησης, γραμμών σετ και εκτυπώσιμης σελίδας προπόνησης |
| `modules/ui.js` | Κοινά UI primitives για escaping, navigation και menu state |
| `i18n.js` | Μεταφράσεις και αλλαγή γλώσσας |
| `theme.js` | Ημέρα ή νύχτα: ανάγνωση της επιλογής πριν το πρώτο paint, διακόπτης και `theme-color` |
| `auth.js` | Authentication gate, λειτουργία επισκέπτη και φόρτωση της εφαρμογής |
| `session-state.js` | Μηχανή των τεσσάρων καταστάσεων συνεδρίας και μοναδικό σημείο μετάβασης ταυτότητας |
| `cloud-sync.js` | Local/cloud merge και versioned synchronization |
| `supabase-client.js` | Φόρτωση και αρχικοποίηση του Supabase browser client |
| `error-tracking.js` | Allowlisted, privacy-safe αναφορά σφαλμάτων μέσω RPC |
| `pwa.js` | Εγγραφή ή local-development cleanup του service worker |
| `service-worker.js` | Offline app shell και runtime caching |
| `privacy.html` / `legal.css` | Δημόσια πολιτική απορρήτου και το ελάχιστο stylesheet της |
| `tests/` | Unit και integration tests |
| `e2e/` | Playwright mobile και accessibility tests· το `e2e/fixtures/authenticated-app.mjs` στήνει συνδεδεμένο χρήστη με πρόγραμμα, προπονήσεις και προφίλ, ώστε κάθε view να χτίζεται πραγματικά κατά τον έλεγχο |
| `supabase/migrations/` | Schema, RLS policies, ημερήσια cloud snapshots, retention και account deletion RPC |
| `.github/workflows/` | CI, Pages deployment και tagged releases |
| `scripts/build-production.mjs` | Ρητό production artifact με minification JS/CSS, χωρίς αλλαγή του development runtime |
| `scripts/check-performance.mjs` / `performance-budget.json` | Mid-range mobile LCP/TBT και όριο μεγέθους app shell με καταγεγραμμένο baseline |

## Supabase και authentication

Οι migrations δημιουργούν τους πίνακες `profiles`, `routines`, `sessions`, το versioned `user_sync_state` και το ιδιωτικό `user_sync_snapshots`. Ενεργοποιούν Row Level Security και περιορίζουν κάθε εγγραφή στον συνδεδεμένο χρήστη. Το snapshot trigger κρατά το πολύ ένα recovery point ανά χρήστη και UTC ημέρα, ενώ καθημερινό Supabase Cron job αφαιρεί όσα είναι παλαιότερα από 30 ημέρες. Τα snapshots δεν εκτίθενται στον client και δεν υπάρχει user-facing επαναφορά.

Οι `profiles`, `routines` και `sessions` είναι phase-one σχήμα που ο σημερινός snapshot client δεν χρησιμοποιεί. Το σχήμα διατηρείται για μελλοντική ρητή migration, αλλά τα Data API privileges έχουν αφαιρεθεί ώστε η αδρανής CRUD επιφάνειά τους να μην είναι προσβάσιμη.

Η πρώτη online φόρτωση απαιτεί επιβεβαιωμένη συνεδρία και επιτυχημένο αρχικό sync. Στη συνέχεια οι αλλαγές γράφονται πρώτα τοπικά. Το UI εμφανίζει την κατάσταση του συγχρονισμού και παρέχει χειροκίνητο **Συγχρονισμό τώρα**. Email/password και Google οδηγούν στον ίδιο λογαριασμό και στο ίδιο sync pipeline· αν η σύνδεση ακολουθεί περίοδο χρήσης ως επισκέπτης, το τοπικό snapshot εισάγεται αυτόματα πριν φορτωθεί η εφαρμογή.

### Canonical URLs

Production app και Supabase Site URL:

```text
https://ddevezoglou.github.io/logbook/
```

Supabase Auth Redirect URLs:

```text
https://ddevezoglou.github.io/logbook/
http://localhost:3000/**
http://localhost:3001/**
```

Google OAuth Authorized JavaScript origins:

```text
https://ddevezoglou.github.io
http://localhost:3000
http://localhost:3001
```

Google OAuth Authorized redirect URI:

```text
https://hixnqtjsjcndeatxhpgd.supabase.co/auth/v1/callback
```

Το Google redirect οδηγεί πρώτα στο Supabase Auth callback. Έπειτα το Supabase επιστρέφει τον χρήστη σε ένα από τα επιτρεπόμενα app Redirect URLs.

## Tests και quality gate

Εγκατάσταση των Playwright browsers:

```powershell
npx.cmd playwright install chromium webkit
```

Γρήγοροι unit και integration έλεγχοι:

```powershell
npm.cmd test
```

Μόνο τα browser tests:

```powershell
npm.cmd run test:e2e
```

Πλήρες release gate:

```powershell
npm.cmd run check
```

Το `npm run check` εκτελεί διαδοχικά:

1. Έλεγχο συνέπειας release metadata.
2. Unit και integration tests.
3. Android Chromium και iOS WebKit end-to-end tests.
4. WCAG accessibility scan στα κρίσιμα mobile flows.
5. Production build και performance budget σε emulated mid-range mobile: app shell ≤ 1.400.000 bytes, LCP ≤ 3.500 ms και TBT ≤ 500 ms.

Το axe συμπληρώνεται από το χειροκίνητο [keyboard-only και screen-reader walkthrough](docs/accessibility-walkthrough.md), επειδή η πρόθεση μιας ανακοίνωσης, η χρησιμότητα της σειράς ανάγνωσης και η επιστροφή του focus δεν αποδεικνύονται μόνο από κανόνες DOM.

Το τρέχον baseline καταγράφεται στο `performance-budget.json` (διάμεσος τριών εκτελέσεων, 4× CPU slowdown, 1.600 Kbps download και 150 ms latency). Εκεί βρίσκονται επίσης τα όρια και το προφίλ· η υπέρβασή τους αποτυγχάνει το `npm run check`. Το `appShellBytes` μετρά όλο το production artifact, μαζί με τις τέσσερις σελίδες απορρήτου και τις άδειες των assets, όχι μόνο τα bytes της αρχικής φόρτωσης.

Το ίδιο gate εκτελείται σε pull requests, deployments του `main` και tagged releases. Το `package-lock.json` παραμένει versioned, ενώ το `node_modules/` δημιουργείται τοπικά.

### Έλεγχος της 0.3.2 — 6 Σεπτεμβρίου 2026

- Release metadata: συνεπής έκδοση **0.3.2** σε package, lockfile, UI και service-worker cache.
- `npm.cmd run check`: **exit 0**, με αυτόματο teardown και χωρίς χειροκίνητο τερματισμό server στην τελική εκτέλεση. Log: `output/release-check-0.3.2-final.log`.
- Unit/integration: **337 passed, 0 failed**. Chromium/WebKit: **74 passed, 2 skipped, 0 failed**. Τα skips παραμένουν τα δύο προβλεπόμενα service-worker σενάρια στο WebKit· το Playwright αποθήκευσε `status: passed`.
- Production build/performance: **1.330.771 / 1.400.000 bytes**, διάμεσο **LCP 1.476 / 3.500 ms**, **TBT 14 / 500 ms**. Το `performance-budget.json` καταγράφει αυτή τη μέτρηση χωρίς αύξηση των ορίων.
- Τα `tests/sync-races.test.mjs` και `tests/deferred-saves.test.mjs` ελέγχουν καθυστερημένα reads/writes, sign-out, αλλαγή ταυτότητας, guest import, ταυτόχρονες προσθήκες/επεξεργασίες/διαγραφές, διπλή ημερομηνία και διατήρηση της φόρμας σε conflict. Το `e2e/sync-conflict.spec.mjs` ελέγχει επίσης αποθήκευση και αποσύνδεση με ενεργό πραγματικό UI σε Chromium/WebKit.
- Το ιστορικό audit του ανεξάρτητου backup της 5ης Σεπτεμβρίου στην 0.3.1 διατήρησε **97 καταγραφές, 412 εγγραφές ασκήσεων, 1.170 σετ**. Δεν επαναλήφθηκε με πραγματικά δεδομένα στο παρόν fix· ο νέος κώδικας ελέγχεται με συνθετικά δεδομένα.

Το `npm run test:e2e` χρησιμοποιεί πλέον το `scripts/run-e2e.mjs`: ανοίγει τον static server ως άμεση Node διεργασία, εκτελεί το Playwright χωρίς δικό του shell server και κλείνει αποκλειστικά τον server που δημιούργησε. Αποφεύγει έτσι το κόλλημα του Windows process-tree teardown. Τα browser stubs αποθηκεύουν κανονικά τα updates και επιστρέφουν τη νέα revision, αντί να δηλώνουν επιτυχία με το προηγούμενο snapshot.

Ο έλεγχος δεν περιλαμβάνει πραγματικές συσκευές, NVDA/VoiceOver ή αναβάθμιση εγκατεστημένης PWA στο production origin. Τα χειροκίνητα βήματα εδώ και στο `docs/accessibility-walkthrough.md` παραμένουν απαραίτητα πριν τη διάθεση. Το τοπικό runtime είναι Node 24.15.0· το CI παραμένει σε Node 22 και πρέπει να περάσει πριν το release. Η παραγωγική βάση ελέγχθηκε στο προηγούμενο στάδιο της 0.3.1, όχι ξανά στο fix της 0.3.2.

### Τι μένει χειροκίνητο στον service worker

Το `tests/service-worker.test.mjs` φορτώνει τον `service-worker.js` σε ελεγχόμενο worker scope και **εκτελεί** τους `install`, `activate` και `fetch` handlers: precache του shell, network-first πλοήγηση, cache fallback ανά διαδρομή, cache-first assets και καθαρισμό παλιών shell caches. Το `tests/pwa.test.mjs` συνεχίζει να ελέγχει τη σύνθεση του app shell ως κείμενο.

Ο πραγματικός worker ενεργοποιείται σε δύο Chromium e2e σενάρια — το offline boot της ρίζας και η φόρτωση της `privacy.html` υπό ενεργό worker, online και offline. Το `pwa.js` ξεγράφει τον worker στο `localhost`, οπότε και τα δύο τον δηλώνουν ρητά μέσω του `logbookLocalWorkerEnabled`.

Ό,τι δεν αποδεικνύεται ακόμη αυτόματα: η πραγματική συμπεριφορά του Cache API κάτω από αναβάθμιση έκδοσης σε production origin. Μετά από αλλαγή στο `CACHE_VERSION` ή στο `APP_SHELL`, η πρώτη φόρτωση σε εγκατεστημένη PWA ελέγχεται χειροκίνητα.

### Windows troubleshooting

Προτιμάται το `npm.cmd run test:e2e`, που διαχειρίζεται αυτόματα τον server και το κλείσιμό του. Για χρήση απευθείας του Playwright CLI, μπορεί να ανοίξει server σε ξεχωριστό terminal:

```powershell
node scripts/serve-static.mjs
```

και εκτέλεσε ξανά σε δεύτερο terminal:

```powershell
npm.cmd run test:e2e
```

Ο ενεργός server επαναχρησιμοποιείται τοπικά και δεν τερματίζεται από το runner. Στο CI η ήδη κατειλημμένη θύρα απορρίπτεται.

## GitHub Pages και PWA

Το `.github/workflows/pages.yml` εκτελεί το πλήρες quality gate σε κάθε push στο `main` και δημιουργεί μέσω του `scripts/build-production.mjs` production artifact από ρητή λίστα runtime αρχείων. Τα JavaScript και CSS assets γίνονται minify με esbuild μόνο για το artifact· η ανάπτυξη συνεχίζει να σερβίρει απευθείας τα αναγνώσιμα source files. Tests, designs, scripts και seed εργαλεία δεν δημοσιεύονται.

Στο baseline της 26ης Ιουλίου το ίδιο allowlisted shell είναι 1.265.953 bytes από τα source files και 1.205.623 bytes στο production artifact: 60.330 bytes ή 4,8% μικρότερο συνολικά, παρότι τα ήδη συμπιεσμένα fonts, icons και το vendored Supabase bundle αποτελούν 675.774 bytes και αντιγράφονται αυτούσια. Ενδεικτικά, μόνο το `app.js` πέφτει από 125.860 σε 98.379 bytes.

Το manifest, τα app icons, οι self-hosted γραμματοσειρές και το offline shell είναι ρυθμισμένα για τη διαδρομή `/logbook/`.

Η πλοήγηση είναι **network-first ανά διαδρομή**: κάθε document ζητείται από το δίκτυο και μόνο σε αποτυχία πέφτει στο cached αντίγραφο *της ίδιας διαδρομής*, με τελευταίο καταφύγιο το `index.html`. Έτσι η `privacy.html` ανοίγει ως δική της σελίδα αντί να αντικαθίσταται από την εφαρμογή, ενώ το offline boot της ρίζας παραμένει. Η απόκριση δικτύου δεν γράφεται στην cache: το shell ανανεώνεται μόνο ολόκληρο στο `install`, ώστε το HTML να μη συνδυαστεί ποτέ με JavaScript άλλης έκδοσης. Τα assets μένουν cache-first.

Η απόφαση να παραμείνει το PWA ο canonical mobile client τεκμηριώνεται στο `docs/mobile-distribution.md`. Η επιλογή native wrapper θα επανεξεταστεί μόνο αν προκύψει πραγματική ανάγκη για app-store distribution, native APIs ή background λειτουργίες που δεν καλύπτει αξιόπιστα το PWA.

Το CSP παραμένει σε `<meta>` επειδή το GitHub Pages δεν επιτρέπει custom HTTP response headers. Περιλαμβάνει `form-action 'self'`, αλλά δεν μπορεί να επιβάλει `frame-ancestors 'none'` από meta policy. Αυτό καταγράφεται ως αποδεκτό χαμηλό ρίσκο όσο το προϊόν μένει στο GitHub Pages: οι ευαίσθητες ενέργειες απαιτούν authentication και ρητή επιβεβαίωση, και δεν υπάρχει καταστροφική one-click διαδρομή. Αν αλλάξει ο host, το CSP μεταφέρεται υποχρεωτικά σε HTTP header και προστίθεται `frame-ancestors 'none'` πριν από τη διανομή.

## Versioning και releases

Το project ακολουθεί Semantic Versioning σε pre-1.0 μορφή:

| Αλλαγή | Παράδειγμα | Χρήση |
|---|---|---|
| Minor | `0.2.0` → `0.3.0` | Νέα λειτουργία ή σημαντική λειτουργική ενότητα |
| Patch | `0.2.0` → `0.2.1` | Διόρθωση ή μικρή συμβατή βελτίωση |
| Prerelease | `0.3.0-alpha.1` | Draft· απαιτεί πρώτα υποστήριξη από το release verifier |
| Stable | `1.0.0` | Πρώτη επίσημη, σταθερή γραμμή προϊόντος |

Ελεγχόμενο version bump χωρίς εγγραφή αρχείων:

```powershell
npm.cmd run version:bump -- <X.Y.Z> --dry-run
```

Αφαίρεσε το `--dry-run` μόνο όταν θέλεις να ενημερωθούν μαζί package metadata, UI version, service-worker cache, tests και ο παρών οδηγός.

Πριν από release πρέπει να συμφωνούν:

- `package.json`
- η root έκδοση του `package-lock.json`
- η εμφανιζόμενη έκδοση στο `index.html`
- το `CACHE_VERSION` στο `service-worker.js`
- τα σχετικά version assertions στα tests
- η έκδοση αυτού του οδηγού

Το τρέχον automated gate δέχεται μόνο την αριθμητική μορφή `X.Y.Z`. Prerelease labels όπως `-alpha.1` δεν πρέπει να χρησιμοποιηθούν πριν ενημερωθούν το `verify-release.mjs` και τα σχετικά tests.

Το `scripts/verify-release.mjs` ελέγχει τη συνέπεια. Tag της μορφής `v<package-version>` ενεργοποιεί το `.github/workflows/release.yml` και δημιουργεί GitHub Release μόνο αν περάσει ολόκληρο το quality gate.

## Υλοποιημένες εργασίες και τεχνική κατάσταση

Κάθε εύρημα έχει σημείο στον κώδικα και κριτήριο ολοκλήρωσης. Τα ευρήματα της 27ης Ιουλίου έκλεισαν όλα και περιγράφονται πλέον ως χαρακτηριστικά στο «Πώς είναι φτιαγμένο». Από το review της 28ης Ιουλίου δεν μένει ανοιχτό εύρημα: τα δύο υψηλής και τα δύο μεσαίας προτεραιότητας υλοποιήθηκαν, και το μοναδικό χαμηλής προτεραιότητας έκλεισε ως απόφαση προϊόντος αντί για αλλαγή κώδικα.

Το review της 29ης Ιουλίου 2026 κάλυψε ασφάλεια, ποιότητα κώδικα και σχεδιασμό μαζί. Άφησε τρία ευρήματα — δύο στο i18n και ένα στην ιεραρχία των controls της πύλης — και τα τρία έκλεισαν την ίδια ημέρα. Κανένα δεν αφορούσε τα δεδομένα ή τον συγχρονισμό. Δεν μένει ανοιχτό τεχνικό εύρημα.

**Ιστορικός έλεγχος ασφάλειας — 29 Ιουλίου.** Η προηγούμενη αναφορά κατέγραψε ελέγχους για RLS, περιορισμένο `search_path`, το `report_client_error` με ταυτότητα από `auth.uid()`, ανάκληση δικαιωμάτων στα αδρανή phase-one tables, CSP χωρίς inline scripts, `escapeHtml` και allowlist των data URLs του avatar. Ο διπλός έλεγχος του owner marker καλύπτεται επίσης από tests απομόνωσης λογαριασμών. Στις 6 Σεπτεμβρίου εντοπίστηκε ότι το RPC σφαλμάτων και το hardening δεν είχαν εφαρμοστεί στην παραγωγή. Οι σχετικές migrations εφαρμόστηκαν την ίδια ημέρα και οι προστασίες επαληθεύτηκαν στο πραγματικό schema.

### Υλοποιήσεις — 29 Ιουλίου 2026

- **Διορθώθηκε το κόλλημα της πύλης στον αρχικό συγχρονισμό.** Προστέθηκαν timeout ανά cloud request, ασφαλές session fallback και επανάληψη με νέο request. Αφαιρέθηκαν τα ορατά diagnostics και η κρυφή modal κατάσταση που έκανε όλη τη σελίδα inert. Τα retry/sign-out είναι πατήσιμα ακόμη και στο error state, ενώ το sign-out λειτουργεί τοπικά και χωρίς δίκτυο.
- **Τα δεδομένα επισκέπτη περνούν αυτόματα στον λογαριασμό.** Η είσοδος ή εγγραφή με email και Google χρησιμοποιεί την ίδια αυτόματη συγχώνευση, χωρίς ερώτηση στον χρήστη. Η εκκρεμής εισαγωγή επιβιώνει αποτυχημένου sync και ολοκληρώνεται στο retry.
- **Η κάρτα του podcast μεταφράζεται ολόκληρη.** Γραφόταν `<strong>PODCAST<br>ΤΗΣ ΕΒΔΟΜΑΔΑΣ</strong>`, οπότε το `<br>` τη σπούσε σε δύο text nodes και το `message.0192` — με source `'PODCAST ΤΗΣ ΕΒΔΟΜΑΔΑΣ'`, με το κενό — δεν ταίριαζε ποτέ με τον κόμβο `ΤΗΣ ΕΒΔΟΜΑΔΑΣ`. Το `<br>` έφυγε και το `<strong>` φέρει πλέον `data-i18n-id="message.0192"`, που λύνεται ανά id αντί για source· η γραμμή σπάει από το πλάτος της κάρτας, ίδια σε κάθε γλώσσα. Η ενότητα `COMMUNITY PREVIEW` παραμένει σχολιασμένη στην αρχική, οπότε το εύρημα ήταν λανθάνον και όχι ορατό: κανένας χρήστης δεν διάβασε ποτέ μισοελληνική κάρτα. Το test εξάγει το διατηρημένο markup από το `index.html`, το αποδίδει και ελέγχει και στις τρεις μεταφρασμένες γλώσσες ότι δεν μένει ελληνικός χαρακτήρας και ότι το `<strong>` δεν παίρνει `lang="el"` — δηλαδή ότι ο screen reader δεν αλλάζει φωνή στη μέση της κάρτας όταν η ενότητα επιστρέψει.
- **Καμία source δεν εμφανίζεται δύο φορές στον κατάλογο.** Το `sourceMessages` είναι `Map` με κλειδί το ελληνικό source, άρα από κάθε ζευγάρι επιβίωνε το τελευταίο για τη διαδρομή `convert()`, ενώ η `translateId` συνέχιζε να λύνει ανά id. Από τα έξι ζευγάρια έμεινε το μικρότερο id (`0037`, `0163`, `0202`, `0330`, `0420`, `0422`), με τις τιμές που ήδη κέρδιζαν στο runtime: `ΕΠΙΛΟΓΗ` → `SÉLECTIONNER`, `Πλάκες` → `Plaques` — η διατύπωση των υπόλοιπων εγγραφών `Πλάκες+…` — και «Να διαγραφεί οριστικά η προπόνηση» → `Permanently delete the workout` / `Das Training endgültig löschen`. Καμία εμφανιζόμενη μετάφραση δεν άλλαξε· έφυγε μόνο η δυνατότητα να διαφωνήσουν οι δύο διαδρομές. Test διαβάζει κάθε id από τον κατάλογο, ζητά το source του από το ίδιο το runtime και αποτυγχάνει σε οποιοδήποτε διπλό.
- **Η σειρά ανάκτησης της πύλης έχει πια ιεραρχία.** Το «ΠΡΟΣΠΑΘΕΙΑ ΞΑΝΑ» μένει `.secondary-button` ως προτεινόμενη ενέργεια· το «ΑΝΟΙΓΜΑ ΣΤΗ ΣΥΣΚΕΥΗ» πήρε το ελαφρύτερο `.account-text-button` και κάθεται από κάτω, αφού το `.auth-gate-recovery-actions` έγινε στήλη. Είναι το ίδιο ιδίωμα που χρησιμοποιεί ήδη η πόρτα του επισκέπτη στην ίδια πύλη — έξοδος κάτω από την προτεινόμενη ενέργεια, όχι δεύτερη ισότιμη επιλογή δίπλα της — με τα ίδια κεφαλαία και αριστερή στοίχιση, χωρίς νέο στοιχείο σχεδιασμού. Το focus μετά το σφάλμα παραμένει στην «ΠΡΟΣΠΑΘΕΙΑ ΞΑΝΑ».

### Υλοποιήσεις — 28 Ιουλίου 2026

#### Υψηλή προτεραιότητα

- **Το merge δεν ανασταίνει πλέον διαγραμμένες εγγραφές.** Οι διαγραφές προπόνησης και προγράμματος γράφουν atomic tombstone `{ id, deletedAt }` στο ίδιο local-first array. Το cloud normalization διατηρεί μόνο το ελάχιστο tombstone, το merge το προτιμά από stale ζωντανό αντίγραφο και η φόρτωση του app το φιλτράρει πριν δημιουργήσει runtime state ή UI. Regression tests καλύπτουν stale συσκευή με ασυγχρόνιστη προσθήκη, upload tombstones, διατήρησή τους σε επόμενες εγγραφές και σωστή προαγωγή ενεργού προγράμματος.
- **Οι προεπιλεγμένες κάρτες της αρχικής δεν επικαλύπτονται στο desktop.** Το `placeHomeCard` ελέγχει τις αρχικές θέσεις σε πλάτος ≥1000px απέναντι στο quote, την κάρτα προφίλ και τα βασικά CTA, και επιλέγει την κοντινότερη έγκυρη μετατόπιση με σταθερό διάκενο. Unit regression και browser QA στα 1000×900 και 1440×900 κλειδώνουν το αποτέλεσμα, χωρίς να αλλάζουν οι αποθηκευμένες θέσεις του χρήστη.

#### Μεσαία προτεραιότητα

- **Το αποτύπωμα του snapshot είναι πλέον 64 bit.** Ο `payloadHash` περνά το κανονικοποιημένο payload από δύο ανεξάρτητες FNV-1a διαδρομές — διαφορετική αφετηρία και διαφορετικός περιττός πολλαπλασιαστής — και επιστρέφει τα δύο αποτελέσματα ως ένα δεκαεξαψήφιο αποτύπωμα. Test κλειδώνει το μήκος, τη σταθερή τιμή για δεδομένο payload και το ότι μια αλλαγή κινεί και τις δύο διαδρομές. Παλιό 32 bit meta hash στη συσκευή απλώς δεν ταιριάζει και κοστίζει ένα αβλαβές ανέβασμα των ήδη τοπικών δεδομένων στον πρώτο συγχρονισμό μετά την αναβάθμιση.
- **Κενό προφίλ δεν μετράει πια ως δεδομένο.** Η `normalizeProfile` επιστρέφει `null` όταν κάθε πεδίο του allowlist λείπει ή είναι κενό, οπότε η `hasMeaningfulData` κρίνει σωστά το άδειο cloud snapshot και δεν επηρεάζει την αυτόματη συγχώνευση των δεδομένων επισκέπτη. Tests καλύπτουν το προφίλ χωρίς κανένα έγκυρο πεδίο, τη διατήρηση ενός ρητά κενού avatar δίπλα σε πραγματικό όνομα, και τον επισκέπτη που φτάνει με μόνο ένα άδειο προφίλ.

#### Κλειστά χωρίς αλλαγή

- **Το «QUOTE OF THE DAY» μένει αγγλικό — δεν είναι εύρημα.** Το review το είχε καταγράψει ως κενό μετάφρασης του `#daily-quote-label` (`index.html:200`). Δεν είναι: ανήκει στο ίδιο τυπωμένο στρώμα με τις σφραγίδες και τα eyebrow labels του τετραδίου, που μένουν σκόπιμα αγγλικά και στις τέσσερις γλώσσες. Ο κανόνας περιγράφεται στο «Αγγλικά που μένουν αγγλικά». Καμία ενέργεια δεν εκκρεμεί.

## Γνωστοί περιορισμοί

- Το sync είναι snapshot-based και όχι live collaborative editing. Υπάρχει optimistic conflict retry και αυτόματο merge με tombstones για τις διαγραφές. Η σχεδιαστική παραδοχή είναι ότι η καταγραφή γίνεται από μία ενεργή συσκευή κάθε φορά, επομένως δεν προβλέπεται UI χειροκίνητης επίλυσης conflicts. Τα tombstones διατηρούνται χωρίς αυτόματο compaction, ώστε ακόμη και συσκευή που έμεινε καιρό offline να μην αναστήσει παλιές εγγραφές.
- Μία προπόνηση ανά ημέρα, επιβεβλημένη client-side και με unique index. Ο περιορισμός καταπολεμήθηκε με την προσθήκη άσκησης σε υπάρχουσα προπόνηση, παραμένει όμως πραγματικός για two-a-days με χωριστά μετρήσιμα σετ.
- Οι ασκήσεις ανήκουν σε προσωπική βιβλιοθήκη. Τα παλιά ακριβώς ίδια ονόματα αντιστοιχίζονται σε κοινή ταυτότητα, ενώ παρόμοια ονόματα και ρητά χωριστοί ομώνυμοι ορισμοί παραμένουν χωριστά. Δεν υπάρχει ακόμη χειροκίνητη συγχώνευση ορισμών.
- Δεν υπάρχει user-facing πλήρες backup/restore. Το CSV export καλύπτει μόνο το ιστορικό προπονήσεων, όχι τα προγράμματα και το προφίλ.
- Μετά το sign-out το user-scoped recovery cache (`logbookCloudCache:<userId>`) παραμένει στο `localStorage` χωρίς λήξη. Δεν είναι προσβάσιμο από το UI και εξυπηρετεί την επαναφορά του ίδιου λογαριασμού, αλλά σε κοινόχρηστη συσκευή είναι αναγνώσιμο από τα devtools. Αποδεκτό local-first trade-off, καταγεγραμμένο ρητά — από την έκδοση 0.3.1 και στην ίδια την πολιτική απορρήτου (§4), μαζί με τη σύσταση εκκαθάρισης δεδομένων ιστοτόπου σε κοινόχρηστη συσκευή.
- Η PWA έχει automated Chromium/WebKit κάλυψη, αλλά χρειάζεται τελική QA σε πραγματικές συσκευές Android και iOS.
- Το performance gate χρησιμοποιεί επαναλήψιμη browser emulation και όχι εργαστηριακή συσκευή· τα όρια προστατεύουν από regressions, αλλά δεν αντικαθιστούν το physical-device QA.
- Στις αλλαγές απορρήτου της 6ης Σεπτεμβρίου 2026 το όριο `appShellBytes` ανέβηκε από 1.300.000 σε 1.400.000 bytes. Η αρχική καταγραφή πριν από τις μεταφράσεις ήταν 1.293.548 bytes, δηλαδή 99,5% του παλιού ορίου. Οι τέσσερις γλωσσικές εκδόσεις διατίθενται και offline· το ενημερωμένο baseline βρίσκεται στο `performance-budget.json`. **Πριν από επόμενη αύξηση ορίου χρειάζεται εξέταση των γραμματοσειρών**, που αποτελούν σημαντικό μέρος του artifact.
- Στο iOS Safari, μια μη εγκατεστημένη σελίδα υπόκειται σε eviction του `localStorage` μετά από επτά ημέρες χωρίς αλληλεπίδραση. Η λωρίδα του επισκέπτη λέει «τα δεδομένα μένουν σε αυτή τη συσκευή»· χωρίς `Προσθήκη στην αρχική οθόνη` αυτό δεν είναι εγγυημένο.

## Κλεισμένες αποφάσεις προϊόντος

Ο σχεδιαστικός άξονας έκλεισε στις 29 Ιουλίου 2026 με τέσσερις ρητές αποφάσεις. Δεν αποτελούν TODO και δεν ζητούν αλλαγή κώδικα.

### Design & προσβασιμότητα

- **Μόνιμη πλοήγηση στο κινητό.** Είχε καταγραφεί ότι και τα έξι `nav-button` ζουν αποκλειστικά μέσα στο συρτάρι (`index.html:123-128`), το οποίο μένει σε `translateX(105%)` όσο είναι κλειστό, οπότε κάθε αλλαγή view κοστίζει δύο taps. Μένει ως έχει: **το μενού αρκεί.** Η αλλαγή view δεν συμβαίνει μέσα στο σετ — εκεί ο χρήστης μένει στην Καταγραφή — αλλά στα διαλείμματα ανάμεσα στις εργασίες, όπου το δεύτερο tap δεν κοστίζει τίποτα πραγματικό. Δεν προστίθεται κάτω μπάρα, ούτε πλαϊνές καρτέλες, ούτε sticky σειρά· η εμφάνιση δεν αλλάζει και τα έξι κουμπιά συνεχίζουν να ζουν στο συρτάρι δίπλα σε λογαριασμό, γλώσσα και ρυθμίσεις.
- **Οι άδειες οθόνες μένουν διαπίστωση.** Είχε καταγραφεί ότι το «Κενό πρόγραμμα / Προσθέστε την πρώτη ημέρα προπόνησης», το «Δεν υπάρχει ορισμένη προπόνηση για Τρίτη» και το κενό ύψος του γραφήματος στην Επίβλεψη είναι κείμενα χωρίς control. Μένουν ως έχουν, μαζί με το ύψος του container του γραφήματος. Σε κάθε μία από τις δύο πρώτες το επόμενο βήμα βρίσκεται ήδη σε απόσταση ενός tap και μέσα στο ίδιο οπτικό πεδίο: το «ΑΝΟΙΓΜΑ ΠΡΟΓΡΑΜΜΑΤΟΣ» κάθεται ακριβώς κάτω από τη γραμμή του κενού προγράμματος (`index.html:186-188`) και ο διακόπτης «Ελεύθερη» ακριβώς πάνω από την περιοχή της προγραμματισμένης προπόνησης (`index.html:242-249`). Ένα δεύτερο κουμπί θα ήταν επανάληψη, όχι διέξοδος.
- **Πρώτη οθόνη με περιεχόμενο για τον επισκέπτη.** Ο σχεδιαστικός γύρος έγινε και τεκμηριώνεται στο [`designs/guest-first-screen-designs.html`](designs/guest-first-screen-designs.html) με τέσσερις εκδοχές: σελίδα-δείγμα, πρόγραμμα-εκκίνησης, χειρόγραφες σημειώσεις περιθωρίου, και υβρίδιο. **Καμία δεν υλοποιείται.** Η Αρχική του νέου χρήστη μένει όπως είναι· το άδειο δωμάτιο είναι αποδεκτό τίμημα για μια λειτουργία που δεν ζητά τίποτα. Το αρχείο παραμένει ως τεκμηρίωση της απόφασης.
- **Η αφετηρία της υπενθύμισης επισκέπτη.** Είχε καταγραφεί ότι η λωρίδα «τα δεδομένα αποθηκεύονται μόνο σε αυτή τη συσκευή» εμφανίζεται πριν ο επισκέπτης καταγράψει οτιδήποτε. Μένει ως έχει: η πληροφορία είναι σωστή από το πρώτο δευτερόλεπτο, η λωρίδα δεν μπλοκάρει και φεύγει με ένα πάτημα, και ο επταήμερος κύκλος δεν αλλάζει.

## Roadmap

Η έκδοση **0.3.2** συγκεντρώνει όλες τις ολοκληρωμένες εργασίες λειτουργίας και σχεδιασμού παρακάτω. Η εκκρεμότητα παραγωγής με τις τρεις migrations έκλεισε στις 6 Σεπτεμβρίου 2026. Τα μόνα ανοιχτά TODO είναι τα **11 προαπαιτούμενα εμπορικής διάθεσης του 1.0**. Περιλαμβάνουν και τεχνική υλοποίηση ή επαλήθευση, όπως πλήρες export/restore, υποδομή πληρωμών και QA σε πραγματικές συσκευές.

### Ολοκληρωμένη αναβάθμιση παραγωγικής βάσης — 6 Σεπτεμβρίου 2026

- [x] **Εφαρμογή και επαλήθευση των τριών migrations στο συνδεδεμένο Supabase:** `202607220001_client_error_events.sql`, `202607250001_security_hardening.sql` και `202607260001_validate_user_sync_payload_size.sql`. Ο πίνακας `client_error_events` και το RPC `report_client_error` υπάρχουν πλέον, το constraint `user_sync_state_payload_size_check` είναι ενεργό και validated, και τα δικαιώματα του `authenticated` στους αδρανείς πίνακες `profiles`, `routines`, `sessions` έχουν ανακληθεί. Και οι τρεις εκδόσεις καταχωρίστηκαν στο migration history.

Προηγήθηκε εκτέλεση ολόκληρου του transaction με **ROLLBACK** στην πραγματική PostgreSQL. Μετά την έγκριση εφαρμόστηκε η ίδια συναλλαγή με SQL assertions: **δύο αμετάβλητα account snapshots**, με ακριβώς ίδια payloads, revisions και timestamps. Τα υπάρχοντα δεδομένα ήταν κάτω από το όριο των 2 MiB και δεν περιείχαν `userProfile.imageGallery`, οπότε δεν αφαιρέθηκε περιεχόμενο.

Ο ανεξάρτητος read-only έλεγχος μετά την εφαρμογή επιβεβαίωσε και τις **8 migrations** του repository, RLS σε όλους τους δημόσιους πίνακες, απαγόρευση άμεσης πρόσβασης στα errors και recovery snapshots, πρόσβαση στο RPC σφαλμάτων μόνο για `authenticated` με κενό `search_path`, validated όριο payload, τέσσερις ενεργούς sync triggers και ενεργό καθημερινό retention job. Το εκτελεσμένο SQL διατηρείται τοπικά ως `output/database/release-0.3.1-pending-migrations.sql` (ιστορικό όνομα), μαζί με το `release-0.3.1-schema-after.json`. Αυτά τα artifacts μένουν εκτός Git και δεν πρέπει να επανεκτελεστούν ως νέο upgrade.

### Ολοκληρωμένες εργασίες της 0.3.1 — λειτουργία και σχεδιασμός

Οι παλιές ετικέτες Task 0.3.0–0.3.4 δήλωναν σειρά εργασιών, όχι ξεχωριστές κυκλοφορίες. Όλες παραδίδονται μαζί ως 0.3.1, με διατήρηση των υπαρχόντων τοπικών και συγχρονισμένων δεδομένων. Τα ονόματα των παλιών αρχείων σχεδιασμού διατηρούνται για αναφορά.

#### Προσωπική βιβλιοθήκη ασκήσεων (αρχικό Task 0.3.0)

**Ολοκληρώθηκε στην έκδοση εργασίας 0.3.1.** Η λειτουργική ροή καλύπτει βιβλιοθήκη → πρόγραμμα → ημέρες → Καταγραφή → Επίβλεψη, με προσθετική μετάβαση και προστασία ιστορικού. Ελέγχθηκαν εξαγόμενα πραγματικά snapshots: 97 καταγραφές, 412 εγγραφές ασκήσεων και 1.170 σετ διατηρούνται χωρίς αλλαγή των ιστορικών τιμών. Η τελική εμφάνιση των Task 0.3.1 και 0.3.2 υλοποιήθηκε στην ίδια έκδοση εργασίας. Τεχνικό συμβόλαιο, κανόνες αντιστοίχισης και καταγραφή εφαρμογής των δύο migrations προστασίας: [`docs/exercise-library-0.3.md`](docs/exercise-library-0.3.md).

- [x] **Νέο μοντέλο άσκησης ως ανεξάρτητης οντότητας.** Ο χρήστης δημιουργεί πρώτα τις δικές του ασκήσεις και στη συνέχεια συνθέτει πρόγραμμα επιλέγοντας από αυτές, αντί να γράφει κάθε άσκηση ως ελεύθερο κείμενο μέσα σε μία ημέρα. Κάθε άσκηση αποκτά σταθερό αναγνωριστικό, ώστε η ίδια άσκηση να μπορεί να επαναχρησιμοποιείται σε πολλές ημέρες και προγράμματα χωρίς να δημιουργούνται ασύνδετα αντίγραφα. *Done όταν:* δημιουργία, επεξεργασία, επιλογή και επαναχρησιμοποίηση άσκησης λειτουργούν τοπικά και με συγχρονισμό, χωρίς απώλεια των παλιών δεδομένων.
- [x] **Μετάβαση των υπαρχόντων δεδομένων στο νέο μοντέλο.** Οι σημερινές ασκήσεις ελεύθερου κειμένου πρέπει να μετατραπούν ή να αντιστοιχιστούν με ασφαλή και επαναλήψιμο τρόπο, διατηρώντας αναλλοίωτο το ιστορικό προπονήσεων. Απαιτούνται migration, backward compatibility και χειρισμός ασκήσεων με ίδιο ή παρόμοιο όνομα. *Done όταν:* παλιό snapshot ανοίγει, συγχρονίζεται και αποθηκεύεται ξανά χωρίς απώλεια, διπλασιασμό ή λανθασμένη συγχώνευση ασκήσεων.
- [x] **Προσαρμογή Προγράμματος και Καταγραφής.** Ο editor προγράμματος επιλέγει ασκήσεις από την προσωπική βιβλιοθήκη και η Καταγραφή φορτώνει τις σωστές αναφορές ανά ημέρα, επιτρέποντας στην ίδια άσκηση να εμφανίζεται όσες φορές χρειάζεται. *Done όταν:* μία άσκηση μπορεί να τοποθετηθεί σε πολλές ημέρες, να καταγραφεί ανεξάρτητα σε καθεμία και να διατηρεί ενιαία ταυτότητα.
- [x] **Αξιοποίηση του νέου μοντέλου στην Επίβλεψη.** Πρώτα επιλέγεται πρόγραμμα και ύστερα προσφέρονται για επιλογή μόνο οι ασκήσεις που ανήκουν σε αυτό, με σωστή συγκέντρωση των καταγραφών της ίδιας άσκησης από διαφορετικές ημέρες. *Done όταν:* τα φίλτρα και τα γραφήματα δεν συγχέουν ομώνυμες ασκήσεις, δεν διπλασιάζουν την ίδια άσκηση και συνεχίζουν να εμφανίζουν το παλιό ιστορικό.
- [x] **Κάλυψη του νέου κύκλου ζωής με tests.** Unit και browser tests για δημιουργία/μετονομασία άσκησης, επαναχρησιμοποίηση σε πολλές ημέρες, αλλαγές προγράμματος, καταγραφή, Επίβλεψη, migration, offline χρήση, συγχρονισμό και merge από δύο snapshots.

#### Ανασχεδιασμός της ενότητας «Πρόγραμμα» (αρχικό Task 0.3.1)

**Ολοκληρώθηκε στην έκδοση εργασίας 0.3.1.** Αριθμημένα ευρετήρια ασκήσεων/προγραμμάτων, ορατές ετικέτες για προβολή πλάνου και προσθήκη προπόνησης, διακριτή δεύτερη γραμμή διαχείρισης. Η ενεργή επιλογή παραμένει σαφής με «Σε ισχύ» και αστέρι.

- [x] **Νέα πληροφοριακή δομή και καθαρότερη ροή.** Η σημερινή παρουσίαση της ενότητας «Πρόγραμμα» μπερδεύει τον χρήστη και πρέπει να επανασχεδιαστεί γύρω από τη σειρά «δημιουργία ασκήσεων → δημιουργία/επιλογή προγράμματος → οργάνωση ημερών». Να ξεχωρίζουν καθαρά η ενεργή επιλογή, η επεξεργασία και οι ενέργειες διαχείρισης. *Done όταν:* ο χρήστης μπορεί να δημιουργήσει ή να αλλάξει πρόγραμμα, να οργανώσει τις ημέρες του και να επαναχρησιμοποιήσει ασκήσεις χωρίς αμφίσημα controls ή κρυφά απαραίτητα βήματα.
- [x] **Responsive και accessibility έλεγχος του νέου UI.** Η νέα ροή πρέπει να λειτουργεί με αφή, πληκτρολόγιο και screen reader, χωρίς overflow ή απώλεια context σε κινητό και desktop.

#### Κουμπιά και οπτική ιεραρχία (αρχικό Task 0.3.2)

**Ολοκληρώθηκε στην έκδοση εργασίας 0.3.1 ως task σχεδιασμού.** Source Sans 3 για το λειτουργικό κείμενο, radius 7px, ήπιες σκιές/μεταβάσεις και σαφείς ρόλοι ενεργειών. Το Ιστορικό έχει ανεξάρτητη γραμμή controls και έναρξη καταγραφής στην κενή κατάσταση. Διατηρούνται η παλέτα, οι slab τίτλοι, οι χειρόγραφες σημειώσεις και το concept του τετραδίου. Inventory και επαλήθευση: [`docs/ui-polish-0.3.md`](docs/ui-polish-0.3.md). Δεν έγινε νέο release ή δημοσίευση.

- [x] **Επανασχεδιασμός των κουμπιών, με προτεραιότητα στο «Ιστορικό».** Να γίνει inventory των ενεργειών και να οριστούν συνεπείς primary, secondary, text και destructive ρόλοι, με καλύτερη μορφή, μέγεθος, απόσταση και οπτική ιεραρχία. Τα χρώματα, η παλέτα και η συνολική αισθητική του Logbook παραμένουν ίδια. *Done όταν:* η κύρια ενέργεια κάθε κατάστασης αναγνωρίζεται άμεσα, οι δευτερεύουσες δεν ανταγωνίζονται οπτικά και όλα τα κουμπιά έχουν συνεπείς hover, focus, active και disabled καταστάσεις.
- [x] **Έλεγχος όλων των καταστάσεων του «Ιστορικού».** Λίστα, φίλτρα, λεπτομέρειες, επεξεργασία, διαγραφή, άδειο αποτέλεσμα και pagination/φόρτωση πρέπει να χρησιμοποιούν την ίδια ιεραρχία κουμπιών και να παραμένουν εύχρηστα σε μικρή οθόνη.

#### Επανεπεξεργασία του theme με τα αυτοκόλλητα (αρχικό Task 0.3.3)

**Ολοκληρώθηκε στην έκδοση εργασίας 0.3.1 ως task σχεδιασμού.** Αφαιρέθηκαν το σύστημα επιβράβευσης τεσσάρων σταδίων (δαχτυλίδι στο Προφίλ και σφραγίδες προγράμματος/συνέπειας στην Αρχική) και η κινητή κάρτα φωτογραφίας προφίλ από την Αρχική. Μοναδική επιβράβευση παραμένει η σφραγίδα «GROWTH HAPPENS IN THE REST, NOT THE DOING», μόνο όταν υπάρχει καταγεγραμμένη προπόνηση για τη σημερινή τοπική ημερομηνία, προγραμματισμένη ή ελεύθερη. Το Προφίλ διατηρεί την επεξεργασία φωτογραφίας και στοιχείων. Δεν έγινε νέο release ή δημοσίευση.

- [x] **Καταγραφή και διόρθωση των προβλημάτων των αυτοκόλλητων.** Να ελεγχθούν όλα τα υπάρχοντα αυτοκόλλητα, οι θέσεις, οι διαστάσεις, τα responsive breakpoints και οι αλληλεπικαλύψεις τους. Όσα είναι λανθασμένα ή δεν εξυπηρετούν πλέον το theme αφαιρούνται.
- [x] **Νέα συνεπής χρήση των αυτοκόλλητων.** Να επανακαθοριστεί πού και γιατί εμφανίζονται, χωρίς να αλλάξει η βασική χρωματική ταυτότητα και χωρίς να ανταγωνίζονται λειτουργικό περιεχόμενο ή controls. *Done όταν:* δεν υπάρχει γνωστό λάθος, ανεπιθύμητη επικάλυψη ή διακοσμητικό στοιχείο που δυσκολεύει ανάγνωση και αλληλεπίδραση στις υποστηριζόμενες διαστάσεις.

#### Πολιτική Απορρήτου και οδηγίες χρήσης (αρχικό Task 0.3.4)

**Ολοκληρώθηκε στην έκδοση εργασίας 0.3.1.** Τα κείμενα εγκρίθηκαν πριν τον κώδικα και τεκμηριώνονται στο [`designs/privacy-instructions-0.3.4-designs.html`](designs/privacy-instructions-0.3.4-designs.html). Η πολιτική έγινε τέσσερα στατικά αρχεία και οι οδηγίες αναθεωρήθηκαν **μόνο στα τέσσερα πάνελ που υπήρχαν ήδη**.

- [x] **Επικαιροποίηση της Πολιτικής Απορρήτου.** Εννέα ενότητες αντί για οκτώ. Νέα η ενότητα 5 «Χρήση χωρίς λογαριασμό (λειτουργία επισκέπτη)», που έλειπε από τότε που υπάρχει η λειτουργία. Προστέθηκαν η βιβλιοθήκη ασκήσεων ως ανεξάρτητη οντότητα, ο snapshot-based συγχρονισμός με τα σημάδια διαγραφής, το τοπικό αντίγραφο πριν τη μετάβαση του 0.3.0, και τι ακριβώς περιέχει και τι δεν περιέχει το CSV. Διορθώθηκε η §4, που έλεγε ότι η αποσύνδεση «αφαιρεί τα cloud cache metadata» ενώ ο `auth.js` κάνει το αντίθετο: πρώτα γράφει το `logbookCloudCache:<userId>` και μετά καθαρίζει τα δεδομένα εργασίας, με το αντίγραφο να μένει χωρίς λήξη. Η σελίδα υπάρχει ως `privacy.html` (ελληνικά, ο σταθερός σύνδεσμος) και `privacy.en.html` / `privacy.fr.html` / `privacy.de.html`, με `hreflang` alternates, ορατούς συνδέσμους μεταξύ τους και ρήτρα ότι υπερισχύει το ελληνικό κείμενο. Ο σύνδεσμος του μενού ακολουθεί το `logbookLanguage` μέσω του `i18n.js`. Οι τρεις σελίδες μπαίνουν στο precache και στο production artifact.
- [x] **Αναθεώρηση των οδηγιών σε κάθε ενότητα.** Αναθεωρήθηκαν τα τέσσερα υπάρχοντα πάνελ `( i )`: **Καταγραφή** (από πού έρχονται οι ασκήσεις μετά τη βιβλιοθήκη), **Πρόγραμμα** (5 γραμμές: η βιβλιοθήκη προηγείται ρητά, η σημείωση ξεχωρίζει τις ομώνυμες), **Επίβλεψη** (5 γραμμές: η σειρά των φίλτρων μετράει, και η οριζόντια κύλιση του γραφήματος που εισήχθη στο 0.3.x), **Προφίλ** (5 γραμμές: μπήκαν η μονάδα μέτρησης και η εξαγωγή ιστορικού, που ζουν στην ίδια οθόνη και δεν αναφέρονταν). Τρεις νέες εγγραφές catalog (`message.0487`–`0489`) και έξι αναθεωρημένες, στις τέσσερις γλώσσες. Οι κενές καταστάσεις δεν απέκτησαν νέο κείμενο: καθεμία καλύπτεται ήδη από την οδηγία της ενότητάς της και η απόφαση της 29ης Ιουλίου 2026 τις κρατά ως διαπίστωση.

**Κλειστό χωρίς αλλαγή:** η **Αρχική**, το **Ιστορικό** και ο **λογαριασμός** δεν αποκτούν οδηγίες. Το αρχείο σχεδίασης είχε προτείνει νέα πάνελ και για τα τρία· απορρίφθηκε στις 6 Σεπτεμβρίου 2026 με τον κανόνα **οδηγίες μπαίνουν μόνο όπου υπάρχουν ήδη**. Παραμένει καταγεγραμμένο ότι στο Ιστορικό η «ΕΠΙΛΟΓΗ» είναι το αόρατο βήμα που αποκαλύπτει επεξεργασία, αντιγραφή και διαγραφή.

### Εμπορικά — προαπαιτούμενα του 1.0

- [ ] **Απόφαση διανομής με εμπορικά κριτήρια.** Επανεξέταση του `docs/mobile-distribution.md` με ερώτημα «από πού έρχεται ο πρώτος πληρώνων χρήστης». Επιλογές: PWA + web διανομή, ή wrapper (Capacitor/TWA) για παρουσία σε Play Store και App Store. *Done όταν:* η απόφαση είναι γραμμένη με ρητό κόστος και ρητό κανάλι απόκτησης χρηστών. **Μπλοκάρει** κάθε επένδυση σε πληρωμές.
- [ ] **Όροι χρήσης**, στο ύφος και τη διαδρομή της υπάρχουσας [πολιτικής απορρήτου](privacy.html).
- [ ] **LICENSE «All rights reserved»** ή μεταφορά σε private repo με hosting που υποστηρίζει ιδιωτική πηγή. Σήμερα ο κώδικας είναι δημόσιος χωρίς καμία άδεια.
- [ ] **Εξαγωγή όλων των δεδομένων του χρήστη** σε ένα μηχαναγνώσιμο αρχείο, με δυνατότητα επαναφοράς. Καλύπτει ταυτόχρονα το GDPR άρθρο 20, την ανάγκη backup του επισκέπτη και τη μετακίνηση μεταξύ συσκευών χωρίς λογαριασμό. Σήμερα υπάρχει `delete_own_account` (άρθρο 17) χωρίς το αντίστοιχο export.
- [ ] **Κανάλι υποστήριξης:** διεύθυνση επικοινωνίας στην πολιτική απορρήτου και στο μενού, και ένα in-app «αναφορά προβλήματος». Απαιτείται από το Google OAuth verification και από κάθε store.
- [ ] **Physical-device QA** σε ένα πρόσφατο Android και ένα iPhone: εγκατάσταση PWA, offline boot, safe areas, virtual keyboard, επιστροφή OAuth, και ολόκληρος ο κύκλος ζωής του επισκέπτη — συμπεριλαμβανομένης της συμπεριφοράς του `localStorage` στο iOS Safari χωρίς εγκατάσταση.
- [ ] **Product analytics με σεβασμό στο privacy:** ελάχιστα allowlisted events (activation, retention, feature use) πάνω στο υπάρχον privacy-safe RPC pattern ή self-hosted Plausible/Umami. Να καλύπτει και τον επισκέπτη, αλλιώς η μέτρηση χάνει ακριβώς το κοινό που πρέπει να μετρηθεί.
- [ ] **Σχεδιασμός freemium ορίων.** Το guest mode όρισε σιωπηλά ένα δωρεάν tier με μηδέν κλειδωμένες λειτουργίες. Δωρεάν: γρήγορη καταγραφή, προσωπική βιβλιοθήκη ασκήσεων και βασικό ιστορικό — το core promise. Premium: προχωρημένα analytics, απεριόριστα προγράμματα, πρόσθετα themes και print.
- [ ] **Επιλογή αγοράς.** Ποια από τις τέσσερις γλώσσες στοχεύεται πρώτη και γιατί. Χωρίς γραμμένη απάντηση, οι τέσσερις γλώσσες είναι κόστος συντήρησης αντί για πλεονέκτημα.
- [ ] **Υποδομή entitlements & πληρωμών:** πίνακας entitlements με RLS και πάροχος φιλικός σε EU sole trader (Lemon Squeezy/Paddle ως merchant of record για ΦΠΑ). Πρώτο βήμα χαμηλού ρίσκου: προαιρετικό «supporter» tier.
- [ ] **Εμφάνιση billing κατάστασης** στο account dialog όταν ενεργοποιηθεί το premium.

## Definition of Done

Μια αλλαγή θεωρείται έτοιμη όταν:

- προστατεύει τα υπάρχοντα τοπικά και συγχρονισμένα δεδομένα,
- δεν εισάγει secrets, tokens ή προσωπικά δεδομένα στο repository ή στα logs,
- συνοδεύεται από migration ή backward compatibility όπου αλλάζει το data model,
- έχει τα κατάλληλα tests και περνά το `npm.cmd run check`,
- ενημερώνει την τεκμηρίωση και το release metadata όταν επηρεάζει τη λειτουργία ή τη διάθεση.

## Κατεύθυνση

Βασική αρχή του project είναι η γρήγορη και αξιόπιστη καταγραφή. Κάθε επόμενο βήμα πρέπει να προστατεύει το ιστορικό, να διατηρεί την τοπική λειτουργία και να αποφεύγει περιττή πολυπλοκότητα κατά τη διάρκεια της προπόνησης.
