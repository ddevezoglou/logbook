# Ασκήσεις και πρόγραμμα — έκδοση 0.3.1

Η λειτουργική ροή βρίσκεται ολόκληρη στην ενότητα **Πρόγραμμα**: βιβλιοθήκη ασκήσεων, δημιουργία/επιλογή προγράμματος, οργάνωση προπονήσεων. Η λειτουργία και η τελική εμφάνιση ολοκληρώθηκαν στην έκδοση εργασίας 0.3.1· δεν έχει γίνει deployment από αυτόν τον έλεγχο. Κατά τον έλεγχο των δεδομένων στις 5 Σεπτεμβρίου ο ένας λογαριασμός είχε ήδη συγχρονίσει το νέο μοντέλο μέσω της εφαρμογής. Δεν εκτελέστηκε μαζική αντικατάσταση των snapshots από εργαλεία SQL.

## Μοντέλο και κανόνες διατήρησης

| Πεδίο | Ρόλος |
|---|---|
| `trainingExercises[]` | Συγχρονισμένη προσωπική βιβλιοθήκη. Κάθε ορισμός έχει `id`, `name`, `cues`, `aliases`, `updatedAt`. Το παλιό `notes` διατηρείται όταν υπάρχει για συμβατότητα. |
| `plan[].exerciseId` | Αναφορά στον ορισμό της άσκησης. |
| `plan[].id` | Ανεξάρτητη θέση στο πρόγραμμα. Δύο εμφανίσεις της ίδιας άσκησης έχουν διαφορετικά slot IDs. |
| `session.exercises[].exerciseId` | Ίδια ταυτότητα άσκησης σε διαφορετικές ημέρες και προγράμματα. |
| `session.exercises[].planExerciseId` | Η θέση προγράμματος από την οποία προήλθε η συγκεκριμένη καταγραφή. |
| `session.exercises[].exercise` | Το ιστορικό όνομα όπως καταγράφηκε. Δεν ενημερώνεται από μετονομασίες της βιβλιοθήκης ή αλλαγές προγράμματος. |

Τα σετ ανήκουν στη θέση του προγράμματος. Τα Cues ανήκουν στον κοινό ορισμό της άσκησης και εμφανίζονται ως read-only στον editor ημέρας. Οι ιστορικές καταγραφές διατηρούν τα δικά τους αποθηκευμένα Cues. Η βιβλιοθήκη επιτρέπει χωριστούς ορισμούς με ίδιο όνομα· οι επιλογείς εμφανίζουν αρίθμηση και Cues για να διακρίνονται. Δεν υπάρχει διαγραφή/συγχώνευση ορισμών σε αυτή την έκδοση.

Το `modules/exercises.js` παρέχει καθαρές συναρτήσεις για migration, δημιουργία/επεξεργασία, ταυτότητα και εμφάνιση. Η εφαρμογή εμφανίζει το τρέχον όνομα από τη βιβλιοθήκη στο πρόγραμμα και στις νέες προγραμματισμένες καταγραφές. Το Ιστορικό συνεχίζει να χρησιμοποιεί τα αποθηκευμένα ονόματα.

## Μετάβαση

1. Διαβάζονται οι υπάρχοντες ορισμοί, τα προγράμματα και το ιστορικό. Δεν χρησιμοποιείται στατικός κατάλογος demo ασκήσεων.
2. Γίνεται αντιστοίχιση ακριβών ονομάτων μετά από περιφερειακό trim, με deterministic `legacy:` IDs. Πεζά/κεφαλαία, τόνοι και παρόμοια ονόματα παραμένουν χωριστά. Ομώνυμες legacy εγγραφές χωρίς άλλη ταυτότητα θεωρούνται μία άσκηση. Υπάρχοντα διαφορετικά `exerciseId` δεν συγχωνεύονται.
3. Σε ομώνυμους ορισμούς, χρησιμοποιείται η θέση του προγράμματος μόνο όταν και το ιστορικό όνομα συμφωνεί. Αμφίσημη καταγραφή παίρνει χωριστή `ambiguous:` ταυτότητα, χωρίς εικασία.
4. Οι μετονομασίες κρατούν aliases, ώστε ένα παλιό snapshot να αντιστοιχίζεται ξανά στον ίδιο ορισμό. Τα αρχικά πεδία ιστορικού, τα IDs προγραμμάτων/καταγραφών και τα tombstones διατηρούνται.
5. Πριν από τις εγγραφές migration δημιουργείται το `logbookExerciseMigrationBackup`: ακριβή raw αντίγραφα των πέντε domain keys, με owner marker. Αν αποτύχει το backup ή η αρχική εγγραφή βιβλιοθήκης, οι επόμενες domain εγγραφές μπλοκάρονται μέχρι επανεκκίνηση με διαθέσιμο χώρο. Η σειρά είναι backup → βιβλιοθήκη → αναφορές. Μια διακοπή ενδιάμεσα επιτρέπει επανάληψη χωρίς νέες ταυτότητες.

Το backup είναι τοπικό βοήθημα ανάκτησης της μετάβασης, όχι πλήρες user-facing export. Καθαρίζεται στο sign-out μαζί με τα domain keys. Τα user-scoped cloud recovery caches διατηρούνται σύμφωνα με την υπάρχουσα πολιτική. Πριν από παραγωγική αναβάθμιση απαιτείται ανεξάρτητο εξαγόμενο αντίγραφο του πραγματικού Supabase snapshot.

## Συγχρονισμός και παραγωγή

Η βιβλιοθήκη συμμετέχει σε payload normalization/hash, meaningful-data detection, merge, guest import και account isolation. Οι ορισμοί συγχωνεύονται ανά ID, με `updatedAt` για τις αλλαγές και διατήρηση aliases. Νεότερο remote snapshot που παραλείπει τη βιβλιοθήκη ανακτά τους ορισμούς από το αντίγραφο του ίδιου owner, διατηρώντας τις remote καταγραφές.

Η πρόσθετη migration `supabase/migrations/202609050001_preserve_exercise_library.sql` εφαρμόστηκε στο συνδεδεμένο Supabase στις 5 Σεπτεμβρίου 2026. Προστατεύει το πεδίο από παλιούς clients που δεν το στέλνουν. Δεν ξαναγράφει υπάρχουσες γραμμές, δεν διαγράφει πίνακες και δεν διευρύνει δικαιώματα. Ο νέος client διατηρεί επίσης τους remote ορισμούς όταν αποθηκεύει άλλες αλλαγές έχοντας χάσει το τοπικό κλειδί βιβλιοθήκης, ακόμη και στην ίδια revision.

**Έλεγχος πραγματικών δεδομένων — 5 Σεπτεμβρίου 2026.** Μέσω του επίσημου CLI έγινε read-only εξαγωγή από το συνδεδεμένο project `hixnqtjsjcndeatxhpgd`. Το αντίγραφο βρίσκεται εκτός repository: `C:/Users/Dimitris/Work/agents/shared/backups/logbook-2026-09-05/user-sync-state-130516.json`, SHA-256 `69570a9bc9d03dec6f8990b8681b7940bcf154d0701f7d15c388dd5bc1d90cbf`.

Το `scripts/audit-exercise-snapshot.mjs` επαλήθευσε πάνω στο εξαγόμενο αντίγραφο ότι διατηρούνται όλες οι ιστορικές τιμές και οι θέσεις προγράμματος, οι αναφορές επιλύονται και η δεύτερη εκτέλεση δεν αλλάζει το αποτέλεσμα:

| Snapshot | Προπονήσεις | Θέσεις πλάνου | Εγγραφές ασκήσεων | Σετ | Ορισμοί πριν → μετά |
|---|---:|---:|---:|---:|---:|
| revision 292 | 41 | 25 | 202 | 456 | 27 → 27 |
| revision 56 | 56 | 15 | 210 | 714 | 0 → 15 |

Το πρώτο snapshot περιείχε ήδη τις νέες αναφορές. Για αυτό δοκιμάστηκε επιπλέον και αντίγραφο με αφαίρεση μόνο των νέων πεδίων: παράγει ξανά 27 ορισμούς, χωρίς αλλαγή ιστορικών τιμών. Αυτό δεν αποτελεί σύγκριση με ανεξάρτητο production backup πριν από την πρώτη αναβάθμιση.

**Εύρημα και αποκατάσταση υποδομής:** η `public.user_sync_snapshots` αρχικά δεν υπήρχε στο συνδεδεμένο project, παρότι η αντίστοιχη migration υπήρχε στο repository και περιγραφόταν στο DEVELOPMENT.md. Η απόπειρα read-only εξαγωγής ημερήσιων snapshots επέστρεψε `42P01`. Στις 5 Σεπτεμβρίου 2026 εφαρμόστηκαν αποκλειστικά οι migrations `202607230001` (ιδιωτικά ημερήσια snapshots με διατήρηση 30 ημερών) και `202609050001` (προστασία βιβλιοθήκης). Προηγήθηκε δοκιμή του πλήρους transaction με ROLLBACK σε PostgreSQL. Κατά την εφαρμογή, SQL assertions επαλήθευσαν ακριβώς ίδια payloads/revisions και ακριβές αντίγραφο ανάκτησης για κάθε λογαριασμό. Αποτέλεσμα: **2 αμετάβλητα account snapshots, 2 recovery snapshots, ενεργό library guard και ενεργό retention job**. Το `scripts/prepare-exercise-db-safety.mjs` παράγει το συγκεκριμένο transaction. Καταχωρίστηκαν μόνο αυτές οι δύο εκδόσεις στο migration history· άλλες εκκρεμείς migrations δεν εφαρμόστηκαν. Το εξαγόμενο αντίγραφο παραπάνω παραμένει ανεξάρτητο.

**Ολοκλήρωση παραγωγικής βάσης — 6 Σεπτεμβρίου 2026.** Ο έλεγχος εντόπισε ότι έλειπαν οι migrations `202607220001`, `202607250001`, `202607260001`: καταγραφή σφαλμάτων, hardening και επικύρωση ορίου payload. Μετά από έγκριση εφαρμόστηκαν σε ένα transaction, αφού πρώτα το ίδιο transaction πέρασε με ROLLBACK. SQL assertions επιβεβαίωσαν ακριβώς ίδια payloads, revisions και timestamps και για τα δύο account snapshots. Το μέγιστο αποθηκευμένο μέγεθος παρέμεινε 94.640 bytes, χωρίς υπέρβαση των 2 MiB ή `userProfile.imageGallery`. Ο τελικός read-only έλεγχος επιβεβαίωσε και τις οκτώ migrations, ενεργό RLS, περιορισμένα δικαιώματα, το RPC σφαλμάτων, validated όριο payload, snapshot triggers, library guard και καθημερινό retention job. Η συγκεκριμένη εκκρεμότητα έκλεισε· ο πλήρης απολογισμός βρίσκεται στο `DEVELOPMENT.md`.

## Συμβόλαιο του ολοκληρωμένου UI

Η νέα φόρμα βρίσκεται πριν από το `.routine-manager` μέσα στο `#plan-view`. Τα νέα styles είναι περιορισμένα στο `.exercise-library`. Η εμφάνιση μπορεί να αντικατασταθεί διατηρώντας τα παρακάτω hooks ή προσαρμόζοντας ρητά τους listeners και τα browser tests:

| Hook | Λειτουργία |
|---|---|
| `#exercise-library-form` | Submit δημιουργίας ή επεξεργασίας· `data-editing-id`, `data-dirty` κρατούν context. |
| `#library-exercise-name`, `#library-exercise-notes` | Όνομα και Cues του ορισμού άσκησης (το παλιό DOM ID διατηρείται). |
| `#exercise-library-list`, `[data-edit-exercise]` | Εναλλασσόμενες κάρτες και επιλογή επεξεργασίας ανά ID. |
| `#cancel-library-edit`, `#exercise-library-status` | Ακύρωση και προσβάσιμη ανακοίνωση αποτελέσματος. |
| `.plan-exercise-fields[data-plan-id] .builder-name` | **Select με value=`exerciseId`**, όχι όνομα. |
| `.builder-sets`, `.builder-cues` | Σετ της συγκεκριμένης θέσης και read-only προβολή των Cues της άσκησης. |
| `[data-open-exercise-library]` | Έξοδος από τον άδειο day editor στη δημιουργία άσκησης. |
| `[data-exercise][data-exercise-id][data-plan-exercise-id]` | Αναφορές της Καταγραφής και του workout draft. |
| `.session-library-exercise` | Προαιρετική επιλογή βιβλιοθήκης σε ελεύθερη/πρόσθετη άσκηση. Η γρήγορη πληκτρολόγηση δημιουργεί ορισμό κατά την αποθήκευση. |
| `#progress-workout` | Επιλογή **προγράμματος** με value=`routineId`. Περιλαμβάνει και ελεύθερες/παλιές καταγραφές. |
| `#progress-exercise` | Ταυτότητες που ανήκουν στο επιλεγμένο πρόγραμμα ή στο ιστορικό του. |

Η Επίβλεψη διατηρεί ασκήσεις που αφαιρέθηκαν από το τρέχον πλάνο αλλά υπάρχουν στο ιστορικό του. Συγκεντρώνει διαφορετικές ημέρες και πολλαπλές εμφανίσεις της ίδιας άσκησης χωρίς να αναμειγνύει διαφορετικά IDs με ίδιο όνομα. Όλο το νέο στατικό κείμενο καλύπτεται σε ελληνικά, αγγλικά, γαλλικά και γερμανικά.

## Έλεγχοι

Ενημέρωση 2026-09-06: τα Cues ανήκουν στο `trainingExercises[].cues`. Ο editor, το πλάνο και οι νέες προγραμματισμένες προπονήσεις τα διαβάζουν από την ίδια ταυτότητα. Κατά τη μετάβαση συνενώνονται οι διακριτές παλιές σημειώσεις και τα Cues προγραμμάτων χωρίς αλλαγή ιστορικού. Η ρητή κενή τιμή δεν ανακτά ξανά παλιό κείμενο. Οι δύο ενότητες ξεκινούν κλειστές και οι κάρτες ασκήσεων μοιράζονται τα εφέ κίνησης των προγραμμάτων, με πλοήγηση μέσω βελών, πληκτρολογίου και σύρσιμο.

- `tests/exercises.test.mjs`: additive/idempotent migration, αμφίσημα ονόματα, aliases, backup/quota, επαναχρησιμοποίηση, reload και πολλαπλές εμφανίσεις στο γράφημα.
- `tests/cloud-sync.test.mjs`: βιβλιοθήκη χωρίς καταγραφές και ανάκτηση μετά από παλιό cloud snapshot, μαζί με τα υπάρχοντα conflict/account/guest tests.
- `tests/exercise-library-migration.test.mjs`: συμβόλαιο της πρόσθετης SQL προστασίας. Δεν υποκαθιστά εκτέλεση σε PostgreSQL.
- `e2e/exercise-library.spec.mjs`: πλήρης ροή βιβλιοθήκης → προγράμματος → δύο ημερών → offline καταγραφής → μετονομασίας → Επίβλεψης → reload, και πληκτρολόγιο/ομώνυμα/accessibility/overflow σε κινητό και desktop.
- Έλεγχος 2026-09-06: release verification, **319 unit/integration passed**, **68 browser tests passed και 2 προβλεπόμενα WebKit service-worker skips**, production build και performance budget. Τα print tests πέρασαν και στους δύο browsers. Το πραγματικό backup ελέγχθηκε ξανά με τον τελικό κώδικα, με ίδιο SHA-256 και αμετάβλητα ιστορικά δεδομένα.
- Performance: production shell **1.326.106 / 1.400.000 bytes**, LCP **1.472 / 3.500 ms**, TBT **10 / 500 ms**. Ο πλήρης απολογισμός, τα όρια του ελέγχου και η ολοκληρωμένη αναβάθμιση παραγωγής βρίσκονται στο `DEVELOPMENT.md`.
